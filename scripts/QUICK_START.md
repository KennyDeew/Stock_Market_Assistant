# Быстрый старт: Отправка тестового сообщения в Kafka

## ⚠️ Важно: Kafka должен быть запущен!

Перед запуском скрипта убедитесь, что **Kafka запущен и доступен**.

### 🚀 Быстрый запуск Kafka:

**Самый простой способ (через .NET Aspire):**
```powershell
cd src\StockMarketAssistant.AppHost
dotnet run
```

**Альтернатива (Docker Compose):**
```powershell
cd src\backend\services\StockCardService
docker-compose -f docker-compose_StockCard.yml up -d kafka zookeeper
# Используйте порт 29091: .\scripts\send_test_kafka_message.ps1 -BootstrapServer "localhost:29091"
```

### Проверка доступности Kafka:

```powershell
# Проверьте, что порт доступен (замените на ваш порт)
Test-NetConnection localhost -Port 9092

# Проверьте процессы Kafka
Get-Process | Where-Object {$_.ProcessName -like "*kafka*"}
# или для Docker
docker ps | Select-String "kafka"
```

📖 **Подробные инструкции:** [QUICK_FIX.md](QUICK_FIX.md) или [KAFKA_SETUP.md](KAFKA_SETUP.md)

## ✅ Рекомендуемый способ (через .NET)

```powershell
# Просто запустите скрипт - он автоматически использует .NET SDK
.\scripts\send_test_kafka_message.ps1
```

Скрипт автоматически:
1. ✅ Проверит доступность Kafka перед отправкой
2. ✅ Проверит наличие .NET SDK (установлен в вашей системе)
3. ✅ Использует C# утилиту для отправки сообщения
4. ✅ Отправит тестовое сообщение в топик `portfolio.transactions`

## 📋 Что произойдет:

1. **Сообщение будет отправлено в Kafka**
2. **AnalyticsService получит сообщение** (если запущен)
3. **В логах появятся записи:**
   ```
   [Info] Получен батч из 1 сообщений
   [Info] Батч транзакций сохранен: 1 транзакций за один запрос к БД
   ```

## 🔍 Проверка результата:

### 1. Проверьте логи AnalyticsService
Ищите записи о получении и обработке батча

### 2. Проверьте базу данных
```sql
SELECT * FROM asset_transactions
ORDER BY transaction_time DESC
LIMIT 5;
```

### 3. Проверьте Consumer Group
```powershell
# Если у вас установлен Kafka CLI
kafka-consumer-groups --bootstrap-server localhost:9092 --group analytics-service-transactions --describe
```

## ⚙️ Дополнительные параметры:

```powershell
# Отправить несколько сообщений
.\scripts\send_test_kafka_message.ps1 -Count 5

# Отправить сообщение о продаже
.\scripts\send_test_kafka_message.ps1 -TransactionType 2

# Использовать другой метод (если .NET не работает)
.\scripts\send_test_kafka_message.ps1 -Method python
.\scripts\send_test_kafka_message.ps1 -Method kafka-cli
```

## 🐛 Если сообщение отправлено, но нет записи в БД

**Проблема:** Сообщение успешно отправлено в Kafka (Offset получен), но запись в таблице `asset_transactions` отсутствует.

### Быстрая диагностика:

```powershell
# Запустите скрипт диагностики
.\scripts\check_kafka_consumer.ps1
```

### Основные причины:

1. **AnalyticsService не запущен**
   - Запустите: `cd src\backend\services\AnalyticsService\AnalyticsService.WebApi && dotnet run`

2. **Строка подключения к БД не заполнена**
   - Проверьте `appsettings.json`: `ConnectionStrings.analytics-db`

3. **Consumer не подписан на топик**
   - Проверьте логи AnalyticsService на наличие "Успешно подписались на топик"

4. **Сообщения уже обработаны (Offset в прошлом)**
   - Отправьте новое сообщение или сбросьте Consumer Group

**📖 Подробная диагностика:** [DIAGNOSTICS.md](DIAGNOSTICS.md)

## 🐛 Если не работает отправка:

1. **Убедитесь, что Kafka запущен:**
   ```powershell
   # Проверьте, что порт 9092 доступен
   Test-NetConnection localhost -Port 9092

   # Если порт недоступен, запустите Kafka (см. KAFKA_SETUP.md)
   ```

   **📖 Подробные инструкции:** [KAFKA_SETUP.md](KAFKA_SETUP.md)

2. **Проверьте конфигурацию** в `appsettings.json`:
   ```json
   {
     "Kafka": {
       "BootstrapServers": "localhost:9092",
       "Topic": "portfolio.transactions"
     }
   }
   ```

