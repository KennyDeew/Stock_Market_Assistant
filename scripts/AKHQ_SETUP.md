# Настройка и использование AKHQ

AKHQ (ранее KafkaHQ) — это веб-интерфейс для управления и мониторинга Apache Kafka.

## 🚀 Быстрый старт

### Запуск AKHQ вместе с Kafka

```powershell
# Перейдите в директорию scripts
cd scripts

# Запустите все сервисы (Kafka, Zookeeper, PostgreSQL, AKHQ)
docker-compose -f docker-compose-analytics.yml up -d

# Проверьте статус контейнеров
docker ps
```

### Доступ к AKHQ

После запуска AKHQ будет доступен по адресу:
- **URL:** http://localhost:8080
- Откройте браузер и перейдите по этому адресу

## 📋 Что можно делать в AKHQ

1. **Просмотр топиков** — список всех топиков Kafka
2. **Просмотр сообщений** — чтение сообщений из топиков
3. **Создание топиков** — создание новых топиков
4. **Управление группами потребителей** — просмотр и управление consumer groups
5. **Мониторинг** — статистика по брокерам, топикам и сообщениям
6. **Отправка сообщений** — отправка тестовых сообщений в топики

## 🔧 Конфигурация

Конфигурация AKHQ находится в файле `scripts/akhq-application.yml`:

```yaml
akhq:
  connections:
    analytics-kafka:
      properties:
        bootstrap.servers: "kafka:29092"
```

### Изменение конфигурации

Если нужно изменить настройки подключения к Kafka:

1. Отредактируйте файл `scripts/akhq-application.yml`
2. Перезапустите контейнер AKHQ:
   ```powershell
   docker-compose -f docker-compose-analytics.yml restart akhq
   ```

## 🛠️ Управление сервисами

### Остановка всех сервисов

```powershell
docker-compose -f docker-compose-analytics.yml down
```

### Остановка только AKHQ

```powershell
docker-compose -f docker-compose-analytics.yml stop akhq
```

### Перезапуск AKHQ

```powershell
docker-compose -f docker-compose-analytics.yml restart akhq
```

### Просмотр логов AKHQ

```powershell
docker-compose -f docker-compose-analytics.yml logs -f akhq
```

## 🔍 Проверка работы

### Проверка доступности AKHQ

```powershell
# Проверка через curl
curl http://localhost:8080/api

# Или через браузер
# Откройте http://localhost:8080
```

### Проверка подключения к Kafka

1. Откройте AKHQ в браузере: http://localhost:8080
2. Выберите кластер "analytics-kafka"
3. Проверьте список топиков — должен отображаться топик `portfolio.transactions` (если он был создан)

## 📝 Примеры использования

### Просмотр сообщений в топике portfolio.transactions

1. Откройте AKHQ: http://localhost:8080
2. Выберите кластер "analytics-kafka"
3. Перейдите в раздел "Topics"
4. Найдите топик `portfolio.transactions`
5. Нажмите на топик для просмотра сообщений

### Отправка тестового сообщения

1. В AKHQ перейдите в топик `portfolio.transactions`
2. Нажмите кнопку "Produce" или "Send message"
3. Введите JSON сообщение:
   ```json
   {
     "id": "test-id",
     "portfolioId": "test-portfolio-id",
     "stockCardId": "test-stock-card-id",
     "assetType": 1,
     "transactionType": 1,
     "quantity": 100,
     "pricePerUnit": 250.75,
     "totalAmount": 25075.00,
     "transactionTime": "2024-01-01T12:00:00Z",
     "currency": "RUB"
   }
   ```
4. Нажмите "Send"

## ⚠️ Решение проблем

### AKHQ не запускается

1. Проверьте, что Kafka запущен:
   ```powershell
   docker ps | Select-String "kafka"
   ```

2. Проверьте логи AKHQ:
   ```powershell
   docker-compose -f docker-compose-analytics.yml logs akhq
   ```

3. Убедитесь, что порт 8080 свободен:
   ```powershell
   netstat -ano | findstr :8080
   ```

### AKHQ не видит Kafka

1. Проверьте, что Kafka доступен внутри сети Docker:
   ```powershell
   docker exec -it analytics-akhq ping kafka
   ```

2. Проверьте конфигурацию в `akhq-application.yml`:
   - `bootstrap.servers` должен указывать на `kafka:29092` (внутренний адрес)
   - Не используйте `localhost:9092` — это адрес для внешнего доступа

### Порт 8080 занят

Если порт 8080 уже используется, измените порт в `docker-compose-analytics.yml`:

```yaml
akhq:
  ports:
    - "8081:8080"  # Используйте другой порт, например 8081
```

Затем перезапустите:
```powershell
docker-compose -f docker-compose-analytics.yml up -d akhq
```

AKHQ будет доступен по адресу: http://localhost:8081

## 📚 Дополнительная информация

- Официальная документация AKHQ: https://akhq.io/
- GitHub: https://github.com/tchiotludo/akhq

