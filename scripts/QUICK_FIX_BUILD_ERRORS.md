# 🔧 Быстрое решение ошибок компиляции MSB3021/MSB3027

## Проблема

Ошибки типа:
```
MSB3021: Unable to copy file "...dll" because it is being used by another process.
MSB3027: The file is locked by: "AnalyticsService.WebApi (38488)"
```

Это означает, что **приложения запущены** и блокируют DLL файлы, которые Visual Studio пытается перезаписать.

## ✅ Быстрое решение

### Способ 1: Автоматическая остановка (рекомендуется)

```powershell
# Запустите скрипт остановки всех сервисов
.\scripts\stop_all_services.ps1
```

Скрипт автоматически:
- ✅ Найдет и остановит все запущенные сервисы
- ✅ Проверит процессы по портам
- ✅ Остановит оставшиеся процессы

### Способ 2: Ручная остановка через Task Manager

1. Откройте **Task Manager** (Ctrl+Shift+Esc)
2. Найдите процессы:
   - `AnalyticsService.WebApi`
   - `Gateway.WebApi`
   - `PortfolioService.WebApi`
   - `StockCardService.WebApi`
   - `StockMarketAssistant.AppHost`
3. Остановите их (End Task)

### Способ 3: Остановка через PowerShell

```powershell
# Остановить конкретный процесс по PID
Stop-Process -Id 38488 -Force

# Или остановить все процессы по имени
Get-Process | Where-Object {$_.ProcessName -like "*WebApi*"} | Stop-Process -Force
```

### Способ 4: Остановка через Visual Studio

1. В Visual Studio нажмите **Stop Debugging** (Shift+F5)
2. Или закройте все запущенные экземпляры приложений

## 🔍 Проверка после остановки

```powershell
# Проверьте, что процессы остановлены
Get-Process | Where-Object {
    $_.ProcessName -like "*WebApi*" -or
    $_.ProcessName -like "*AppHost*"
} | Select-Object ProcessName, Id
```

Если список пуст - все процессы остановлены.

## 🚀 После остановки

1. **Пересоберите проект** в Visual Studio (Build → Rebuild Solution)
2. Или через командную строку:
   ```powershell
   dotnet build
   ```

## ⚠️ Предотвращение проблемы

Чтобы избежать этой проблемы в будущем:

1. **Останавливайте приложения перед пересборкой**
2. **Используйте скрипт остановки** перед сборкой:
   ```powershell
   .\scripts\stop_all_services.ps1
   dotnet build
   ```
3. **Настройте Visual Studio** на автоматическую остановку перед сборкой:
   - Tools → Options → Projects and Solutions → Build and Run
   - Установите "On Run, when projects are out of date" → "Always build"

## 📋 Список процессов, которые нужно остановить

- `AnalyticsService.WebApi` (PID: 38488)
- `Gateway.WebApi` (PID: 73808)
- `PortfolioService.WebApi` (PID: 63192)
- `StockCardService.WebApi` (PID: 71432)
- `StockMarketAssistant.AppHost` (если запущен через Aspire)

